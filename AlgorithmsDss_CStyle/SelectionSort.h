#pragma once


namespace AlgorithmsDss_CStyle
{
    // Perform selection sort on the array of integers (from smallest to largest).
    // Parameters: arr   - array to sort;
    //             count - number of elements in the array.
    void selectionSort(int arr[], size_t count);
}
